# Repository instructions

Follow the repository signature standard in `docs/IAMUVIN-SIGNATURE.md` for every implementation, modification, completion, shipping, or deployment task.

Before declaring work complete, run:

```bash
bash ./scripts/verify-signature.sh
```

The final output line must be `SIGNED`.
